# Model Card: {{ model_name }}

> **Governance Status**: [ Experimental | Staging | Production | Deprecated ]  
> **Physics Compliance Level**: [ Level 0 (Data-Only) | Level 1 (Soft Constraints) | Level 2 (Hard Projection) ]

---

## 1. Model Details
* **ID**: `{{ model_id }}`
* **Version**: `{{ version }}`
* **Date**: `{{ timestamp }}`
* **Architecture**: {{ architecture_type }} (e.g., Hybrid LSTM-PINN, DeepONet, PC-TimeGAN)
* **Framework**: PyTorch {{ torch_version }}
* **Author**: Team D.E.C.O.D.E AI Research
* **License**: Proprietary / Enterprise SaaS

## 2. Intended Use
### Primary Use Case
* **Function**: {{ function_description }} (e.g., "Predicts Solar GHI 24h ahead while enforcing non-negativity.")
* **Operational Boundary**: Designed for {{ grid_topology }} (e.g., IEEE 14-Bus Microgrid).
* **Latency Requirement**: < {{ max_latency_ms }} ms per inference.

### Out-of-Scope Use Cases
* **Do NOT use for**: 
    * Protection relay logic (sub-cycle faults).
    * Long-term climate modeling (> 7 days).
    * Grid topologies significantly different from {{ training_topology }}.

## 3. Physics Integrity Report (Critical)
*Unlike standard AI, this model is evaluated on its adherence to natural laws.*

| Metric | Value | Threshold | Status |
| :--- | :--- | :--- | :--- |
| **Data Loss (MSE)** | {{ val_mse }} | < 0.05 | {{ mse_status }} |
| **Physics Residual (Total)** | {{ phys_residual }} | < 1e-4 | {{ phys_status }} |
| **KCL Violation Rate** | {{ kcl_violation_pct }}% | 0.0% | {{ kcl_status }} |
| **Thermodynamic Violation** | {{ thermal_violation_cnt }} | 0 | {{ thermal_status }} |

* **Governing Equations**:
    * [Swing Equation](../../physics-core/equations/swing_equation.py)
    * [Power Flow (Kirchhoff)](../../physics-core/equations/power_flow.py)

## 4. Training Data & Provenance
* **Dataset Version (DVC)**: `{{ dvc_hash }}`
* **Source**:
    * Real-world: NREL NSRDB (Solar), SCADA Historian (Load)
    * Synthetic: PC-TimeGAN generated "Black Swan" scenarios
* **Preprocessing**: 
    * Normalization: `PhysicsPreservingScaler` (Preserves GHI/DNI ratios)
    * Sequence Length: {{ seq_len }} hours

## 5. Performance & Limitations
### Metrics
* **Test Accuracy (R²)**: {{ r2_score }}
* **Inference Speed**: {{ avg_latency }} ms on {{ hardware_device }} (e.g., NVIDIA Jetson Nano)

### Known Limitations
* **Bias**: Performance degrades if Grid Inertia (H) deviates by > 20% from nominal.
* **Edge Cases**: May underpredict during "Cloud Cover Gorilla" events if not fine-tuned on chaos scenarios.

## 6. Ethical & Safety Considerations
* **Safety Criticality**: This model provides *advisory* setpoints. It is wrapped in a hard-coded safety layer (Projector) before actuating grid hardware.
* **Environmental Impact**: Optimized for ARM deployment to minimize inference energy cost.

---
*Auto-generated by D.E.C.O.D.E MLOps Pipeline on {{ generation_date }}*
